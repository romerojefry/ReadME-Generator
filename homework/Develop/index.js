const inquier = require("inquirer");
const fs = require("fs");
//const axios = re('axios');

const questions = [{
    // type: "input",
    // name:"title",
    //message: "whats is your project title?"
};
{
    
}
];

function writeToFile(fileName, data) {
}

function init() {

}

init();
